import { GoogleTagManager } from "@next/third-parties/google";
import { Inter } from "next/font/google";
import { ToastContainer } from "react-toastify";
import "react-toastify/dist/ReactToastify.css";
import Footer from "./components/footer";
import Navbar from "./components/navbar";
import "./css/card.scss";
import "./css/globals.scss";
import ScrollToTop from "./components/helper/scroll-to-top";
const inter = Inter({ subsets: ["latin"] });

export const metadata = {
  title: "Portfolio of Navin - UI/UX DESIGNER",
  description:
    "I'm a self-taught UI designer passionate about creating simple, user-friendly experiences. I believe that gooddesign should be minimal, easy to understand, and enjoyable to use. I focus on making interfaces that arevisually appealing while ensuring they are intuitive and accessible for all users. I understand and apply key UIdesign principles in every project, always striving to improve the overall experience for users. My goal is tocreate designs that make people feel comfortable and confident while interacting with a product, without unnecessary complexity.",
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <ToastContainer />
        <main className="min-h-screen relative mx-auto px-6 sm:px-12 lg:max-w-[70rem] xl:max-w-[76rem] 2xl:max-w-[92rem] text-white">
          <Navbar />
          {children}
          <ScrollToTop />
        </main>
        <Footer />
      </body>
      <GoogleTagManager gtmId={process.env.NEXT_PUBLIC_GTM} />
    </html>
  );
}
